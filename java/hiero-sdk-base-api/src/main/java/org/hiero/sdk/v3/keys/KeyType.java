// Source: spec/base/keys.md (keys#KeyType).

package org.hiero.sdk.v3.keys;

/** Key material classification. */
public enum KeyType {
    /** Public key material. */
    PUBLIC,
    /** Private key material. */
    PRIVATE
}
